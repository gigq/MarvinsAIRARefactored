# Repo Notes

## Release Notes

- The in-app update window renders release notes as plain text via a WPF `TextBlock`.
- GitHub markdown and HTML are not rendered in that dialog.
- Write GitHub release notes so they still read cleanly as plain text when shown in-app.
